[package]
name = "crosslab-mobile-ffi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
crate-type = ["lib", "cdylib"]

[features]
bindgen = ["uniffi/cli", "uniffi/cargo-metadata"]
development-provisioning = ["crosslab-transport-quic/development-provisioning"]

[dependencies]
crosslab-agent = { path = "../agent" }
crosslab-core = { path = "../core" }
crosslab-crypto = { path = "../crypto" }
crosslab-identity = { path = "../identity" }
crosslab-identity-store = { path = "../identity-store" }
crosslab-policy = { path = "../policy" }
crosslab-protocol = { path = "../protocol" }
crosslab-runtime = { path = "../runtime" }
crosslab-transport-quic = { path = "../../transports/quic" }
tokio = { workspace = true, features = ["macros", "rt", "sync", "time"] }
uniffi.workspace = true

[[bin]]
name = "crosslab-uniffi-bindgen"
path = "src/bin/uniffi_bindgen.rs"
required-features = ["bindgen"]

[dev-dependencies]
rcgen.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "time"] }
