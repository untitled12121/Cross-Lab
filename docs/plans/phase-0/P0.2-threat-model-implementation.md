# P0.2 Threat Model and Trust Boundaries Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the approved P0.2 design into the normative Cross-Lab threat model and developer-facing security-boundary reference.

**Architecture:** Cross-Lab uses a foundation-first threat model. The analysis covers the identity, network, authorization, control/data-plane, agent/platform, privileged-service, recovery, optional infrastructure, update, audit/privacy, and resource-safety boundaries inherited by all later capabilities while deferring feature-specific and platform-specific detail.

**Tech Stack:** Markdown, Git, GitHub.

**Spec:** `docs/plans/phase-0/P0.2-threat-model-design.md`

## Global Constraints

- Preserve the architectural invariants in `docs/architecture/MASTER-ARCHITECTURE.md`, Revision 2.0.
- Do not select exact signature, KDF, AEAD, PAKE, handshake, certificate, wire-serialization, or platform IPC mechanisms in P0.2.
- Treat transport/library identifiers as untrusted implementation details, not Cross-Lab identity.
- Treat trusted peers as constrained principals rather than universally authorized principals.
- Revalidate sensitive operations at every authority boundary.
- Keep recovery authority independent from ordinary trust.
- Model denial of service and resource exhaustion as security concerns.
- Do not claim implementation guarantees that do not yet exist.

---

### Task 1: Write the normative security boundary reference

- [x] Define the remote-input, authentication, trust/policy, control/data-plane, platform-adapter, privileged IPC, recovery, relay/hub, and update boundaries.
- [x] State the required fail-closed checks at each boundary.
- [x] State which data/authority must never cross each boundary implicitly.
- [x] Define the canonical authorization flow from untrusted input to ordinary or privileged execution.
- [x] Verify that no concrete transport or platform library becomes a domain authority.
- [x] Commit with `docs(security): define Cross-Lab security boundaries`.

### Task 2: Write the foundation threat model

- [x] Define protected authority, authorization, protocol/session, user-data, platform, update, and audit assets.
- [x] Define legitimate actors, threat actors, attacker capabilities, and out-of-scope compromise assumptions.
- [x] Define threat scenarios for spoofing, pairing abuse, replay, downgrade, tampering, confused deputy, stream bypass, stale authorization, compromised peers, privilege escalation, relay/hub compromise, resource exhaustion, privacy leakage, update compromise, recovery abuse, and route/reconnect weakening.
- [x] Give each material threat a stable `TM-###` identifier.
- [x] Map every threat to required controls and a future verification/test obligation.
- [x] Record residual risks and explicitly deferred decisions without placeholders.
- [x] Verify that every Master Architecture security invariant appears as a threat-model property.
- [x] Commit with `docs(security): add foundation threat model`.

### Task 3: Verify and checkpoint P0.2

- [x] Cross-check both documents against the Master Architecture and approved P0.2 design.
- [x] Confirm that no deferred cryptographic or wire-format choice was silently selected.
- [x] Confirm that future tests can be traced from threat IDs and security properties.
- [x] Update `CURRENT.md` to mark P0.2 complete and set P0.3 identity/key hierarchy as the exact next task.
- [x] Record documentation-only verification accurately; Cargo checks remain not applicable until Phase 1 workspace creation.
- [x] Commit with `docs: checkpoint P0.2 threat model`.

## Verification result

P0.2 completed on `foundation/phase-0-to-1`. The threat model defines stable `TM-###` identifiers and the security-boundary reference defines B1-B12. Exact cryptographic algorithms, credential formats, pairing transcript, wire serialization, and platform IPC mechanisms remain intentionally unresolved for their later Phase 0 milestones.
