name: Pairing Lock Maintenance

on:
  push:
    branches:
      - product-pairing-ui
    paths:
      - .github/workflows/pairing-lock-maintenance.yml

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: product-pairing-ui
      - name: Install pinned Rust
        run: rustup toolchain install 1.98.1 --profile minimal
      - name: Refresh lockfile deterministically
        run: cargo metadata --no-deps --format-version 1 >/dev/null
      - name: Commit generated lockfile and remove maintenance workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/pairing-lock-maintenance.yml
          git add Cargo.lock
          git diff --cached --check
          git commit -m "build(pairing): refresh generated lockfile"
          git push origin HEAD:product-pairing-ui
